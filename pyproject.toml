[build-system]
requires = ["setuptools>=64", "setuptools_scm>=8"]
build-backend = "setuptools.build_meta"

[project]
name = "muscle3-dashboard"
description = "Panel dashboard for MUSCLE3 simulations"
readme = {file = "README.md", content-type = "text/markdown"}
authors = [
    {name = "Daan van Vugt", email = "dvanvugt@ignitioncomputing.com"},
    {name = "Mike Sanders", email = "msanders@ignitioncomputing.com"},
]
classifiers = [
]
license = {file = "LICENSE.txt"}
dependencies = [
    "click",
    "panel",
]
dynamic = ["version"]
requires-python = ">=3.11"

[project.optional-dependencies]
graph = [
    # Renders the simulation graph card; not on PyPI yet, pulled from git.
    "ymmsl2svg @ git+https://github.com/DaanVanVugt/ymmsl2svg.git@feat/conduit-hover-labels",
]
recording = [
    # Shared State/Plotter + Zarr recorder base for live/replay tabs.
    "numpy",
    "xarray",
    "zarr",
    "holoviews",
    "param",
    # Recorder.handle()/run_recorder_actor() need a real muscle3 actor runtime.
    "muscle3>=0.10",
]
dev = [
    "ruff",
    "pytest",
    "pytest-cov",
]

[project.urls]
homepage = "https://github.com/multiscale/muscle3-dashboard"

[tool.setuptools.packages.find]
where = ["."]
include = ["muscle3_dashboard*"]

[project.scripts]
m3dash = "muscle3_dashboard.m3dash.cli:main"

[tool.setuptools_scm]
local_scheme = "no-local-version"

[tool.pytest.ini_options]
testpaths = [
    "tests",
]

[tool.ruff]
include = ["pyproject.toml", "muscle3_dashboard/**/*.py", "tests/**/*.py"]
exclude = [
    "docs",
    ".git",
]
# Same as Black
line-length = 88
indent-width = 4

[tool.ruff.lint]
select = [
    # pycodestyle
    "E",
    # Pyflakes
    "F",
    # pyupgrade
    "UP",
    # flake8-bugbear
    "B",
    # flake8-simplify
    "SIM",
    # isort
    "I",
]
